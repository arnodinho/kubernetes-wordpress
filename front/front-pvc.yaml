apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: front-pvc
  namespace: egtdem
  labels:
    type: local
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
